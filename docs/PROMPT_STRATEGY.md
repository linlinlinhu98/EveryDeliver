# EveryDeliver — 小浣熊全链路开发提示词策略

> 面向 OPC 能力挑战赛：使用商汤小浣熊家族（代码小浣熊 + 办公小浣熊）从零构建项目
> 核心理念：**OPC = One Person Capability（一个人 + AI = 一支团队）**

---

## 0. 开局宣言（每轮对话开场白）

```
我是[你的名字]，正在参加商汤小浣熊 OPC 能力挑战赛。

【我的工具】
• 代码小浣熊 VSCode 插件 — 代码生成/补全/重构/跨文件修改/测试生成
• 办公小浣熊桌面版 — 本地文件处理/数据分析/PPT生成/一图读懂/浏览器自动化/定时任务

【我要做的事】
构建"EveryDeliver"求职投递辅助平台：
• 8 大功能模块，三端架构（桌面+插件+小程序）
• 技术栈：Tauri + Supabase + DeepSeek-V3/Qwen

【我的设计文档已就绪】
请先阅读 d:/ALLPrograming/OPC/docs/DESIGN.md 和 ARCHITECTURE.md 理解完整设计。
```

---

## 1. 总体策略：代码小浣熊 + 办公小浣熊双线协作

```
                    项目总指挥（你）
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
  ┌───────────┐  ┌─────────────┐  ┌────────────┐
  │ 代码小浣熊  │  │ 办公小浣熊    │  │ 办公小浣熊  │
  │ VSCode插件 │  │ 桌面版       │  │ 桌面版      │
  ├───────────┤  ├─────────────┤  ├────────────┤
  │ 写代码     │  │ PPT制作      │  │ 数据分析    │
  │ 调试       │  │ 项目简介     │  │ 竞品调研    │
  │ 重构       │  │ 一图读懂     │  │ 浏览器自动化 │
  │ 测试       │  │ 飞书文档     │  │ 定时任务    │
  └───────────┘  └─────────────┘  └────────────┘
       │               │               │
       └───────────────┴───────────────┘
                       │
                       ▼
              GitHub 仓库 + PPT + 项目简介
```

### 每一 Phase 的标准流程

```
Step 1: 在代码小浣熊输入提示词 → 生成代码
Step 2: 代码小浣熊 → 代码重构/测试生成
Step 3: 验证通过 → git commit & push
Step 4: 用办公小浣熊更新 PPT（每完成一个 Phase）
Step 5: 用办公小浣熊的浏览器自动化做竞品/技术调研
```

---

## 2. 代码小浣熊提示词库（分 Phase，直接复制使用）

### Phase 0：基础设施搭建

```
【角色设定】
你是一个全栈软件架构师和资深 Tauri 开发者。我正在进行"EveryDeliver"求职投递辅助平台
的 MVP 开发，参加商汤小浣熊 OPC 能力挑战赛。

【项目背景】
完整设计文档在 d:/ALLPrograming/OPC/docs/DESIGN.md 和 ARCHITECTURE.md。
请先读取这两个文件理解完整设计。

【当前任务：Phase 0 基础设施搭建】

1. 初始化 Tauri v2 桌面应用
   - 在 d:/ALLPrograming/OPC/ 目录下使用 Tauri v2 + React + TypeScript
   - 窗口标题设为"EveryDeliver"
   - 窗口默认大小 1200×800

2. 配置 Supabase 项目
   - 在 d:/ALLPrograming/OPC/supabase/ 创建数据库迁移 SQL
   - 包含以下所有表（参考 ARCHITECTURE.md 中的完整 SQL）：
     - profiles, resumes, resume_versions, module_types (预填8类模块)
     - module_templates, resume_module_instances, resume_snapshots
     - preferences, job_positions, jd_parse_results, jd_parse_snapshots
     - applications, application_status_history, interview_prep_items
     - duplicate_keys
   - 所有表启用 RLS：USING (auth.uid() = user_id)
   - 预填 module_types 表的 8 类数据

3. 初始化浏览器插件骨架
   - 在 d:/ALLPrograming/OPC/plugin/ 创建 Manifest V3 项目
   - content-script.ts: 页面 DOM 监听入口
   - popup.html + popup.ts: 弹出面板

4. 实现 Supabase Auth 登录注册
   - 桌面端登录页面（邮箱+密码）
   - 注册后自动创建 profiles 记录
   - 登录成功跳转到主页面

【代码要求】
- 使用 TypeScript 严格模式
- 每个函数写 JSDoc 注释
- 错误处理完整（不要 any 类型）
- 前端组件使用 React Hooks

【输出要求】
先列出你将要创建/修改的所有文件清单，我确认后再逐个实现。
```

---

### Phase 1：用户画像与简历

```
【接续 Phase 0，代码小浣熊 VSCode 插件模式】

当前已完成：Tauri 骨架 + Supabase + Auth。请读取现有代码后再开始。

【Phase 1 任务：用户画像与简历】

任务 1 — PDF/Word 简历上传与解析
  - 桌面端：拖拽上传组件（react-dropzone）
  - 支持 PDF、Word(.docx)、纯文本
  - 文件上传到 Supabase Storage
  - 调用 Supabase Edge Function 解析文档文本
  - 解析结果存储到 resumes 和 resume_versions 表

任务 2 — Agent 对话式简历填写
  - 对话 UI 组件（聊天气泡样式）
  - AI 逐步引导：教育背景 → 技能 → 项目经历 → 实习 → 获奖
  - 调用后端 LLM 代理 Edge Function
  - 填写完成结构化存储

任务 3 — 隐私字段保护（三级分级）
  - Level 1（手机号/身份证号/籍贯/生日）：本地 AES 加密，不上传
  - Level 2（姓名/邮箱/脱敏手机号）：加密上传，不送 LLM
  - Level 3（技能/项目/年限/学历）：可送 LLM
  - 实现加密工具类（crypto-js）

任务 4 — 求职偏好设置
  - 白名单表单：公司关键词/行业(多选)/规模(下拉)/月薪(滑块)/城市(多选)
  - 黑名单表单：公司名(标签输入) + 禁止标签(多选)
  - 自动检测配置面板

【代码要求】
- 同上 Phase 0
- 表单需要完整的前端校验

【先列出文件清单，确认后逐个实现】
```

---

### Phase 2：JD Parser

```
【接续 Phase 1，代码小浣熊 VSCode 插件模式】

请先读取现有代码和 docs/DESIGN.md 模块 G 的完整设计。

【Phase 2 任务：JD Parser — 核心模块】

任务 1 — JD 数据模型
  - 检查 Phase 0 是否已创建 job_positions / jd_parse_results / jd_parse_snapshots 表
  - 如未创建，执行迁移 SQL

任务 2 — BOSS 直聘规则解析器
  - 根据 BOSS 直聘 DOM 结构编写 CSS 选择器提取逻辑
  - 字段：薪资/地点/学历/经验年限/公司名
  - 注意 BOSS 薪资格式如"15K-25K"
  - 返回置信度 0-1

任务 3 — 猎聘规则解析器
  - 同上适配猎聘 DOM 结构
  - 猎聘薪资格式不同，需适配

任务 4 — LLM 解析 Edge Function
  - POST /functions/v1/parse-jd
  - 接收原始 JD 文本 → DeepSeek-V3 解析 → 结构化 JSON
  - 触发条件：规则置信度 < 0.7 | 必填字段缺失 | JD < 50字 | JD > 5000字

任务 5 — 三层解析编排逻辑
  - 规则解析 → 置信度检查 → (不足则降级) LLM解析 → 返回结果
  - 同 URL hash 缓存避免重复解析

任务 6 — 技能词表
  - 初始技能词表 JSON（直接相关 vs 通用技能）
  - 正则 + 词表提取
  - 权重标记

任务 7 — JD 生命周期
  - active(默认) / expiring_soon(30天) / expired(停招)
  - 重抓覆盖保留旧快照

【代码要求】
- 规则解析器需要单元测试（至少覆盖 10 个真实 JD 样本）
- Edge Function 需要错误处理和超时控制

【先列出文件清单】
```

---

### Phase 3：职位获取与过滤

```
【接续 Phase 2，代码小浣熊 VSCode 插件模式】

请先读取现有代码和 docs/DESIGN.md 模块 B 的完整设计。

【Phase 3 任务：职位获取与过滤】

任务 1 — 手动职位导入 UI
  - 表单：公司名/岗位标题/JD文本(multiline)/来源URL/来源平台(下拉)
  - 粘贴后实时预览解析结果
  - 录入触发 JD Parser

任务 2 — 浏览器插件 BOSS 直聘抓取
  - Content Script 检测职位详情页 URL
  - 提取：公司名/标题/JD文本/薪资/地点/URL
  - Popup 显示抓取预览
  - "导入"按钮 → 调用 Supabase API 写入

任务 3 — 浏览器插件猎聘抓取
  - 同上适配猎聘

任务 4 — 合规告知弹窗
  - 首次安装显示告知弹窗（内容参考 DESIGN.md 模块B）
  - 存储"已阅读"状态到 chrome.storage
  - 每月重提示

任务 5 — 入库质检
  - 必填字段检查 / 格式检查 / 完整性评分 0-1
  - ≥0.8入库 / 0.5-0.8待补全(橙色) / <0.5拒绝

任务 6 — 三层过滤引擎
  - 第1层：黑名单命中→丢弃
  - 第2层：duplicate_key重复→移入重复列表
  - 第3层：薪资/城市不匹配→标记不删除

任务 7 — 黑名单自动检测
  - JD关键词匹配（正则引擎，参考 DESIGN.md 的正则表）
  - 行业分类警告
  - 历史行为推断
  - 检测结果弹窗

任务 8 — 可勾选职位表格
  - 列：□勾选/标题/公司/薪资/来源/匹配度/位置/操作
  - 排序/筛选/多选/批量操作
  - 匹配度颜色编码(绿>70%/黄40-70%/红<40%)

【先列出文件清单，确认后逐个实现】
```

---

### Phase 4：匹配度评估与简历优化

```
【接续 Phase 3，代码小浣熊 VSCode 插件模式】

请先读取现有代码和 docs/DESIGN.md 模块 C 的完整设计。

【Phase 4 任务：匹配度评估与 AI 优化】

任务 1 — 匹配度评估引擎
  - 实现公式：硬性(60%) + 软性(30%) + 隐性(10%)
  - 硬性：学历+经验+技能命中（直接相关×2.0，通用×0.5）
  - 软性：项目相关性+行业重合度+语义相似度
  - Edge Function: POST /functions/v1/match-resume (DeepSeek-V3)

任务 2 — 匹配度展示 UI
  - 圆形进度条 + 技能缺口雷达图
  - 缺失技能(红色) / 匹配技能(绿色)

任务 3 — AI 简历优化 Edge Function
  - POST /functions/v1/optimize-resume (Qwen-Plus)
  - 输入：原始简历 + JD解析结果 + 改动模式
  - 输出：修改版简历 + 改动列表(位置/原文/新文/颜色/说明)

任务 4 — 改动风险分级（5级）
  - 🟢措辞优化 / 🟡侧重点调整 / 🟠GitHub化用 / 🔴新增关键词 / 🔴新增经历

任务 5 — 改动高亮渲染
  - 黄色(已有改写) / 橙色(外部参考) / 红色(新增)
  - 每处可展开看原因+来源+面试建议

任务 6 — 双模式系统（询问/自动）
  - 自动模式按风险等级自动处理
  - 模式随时切换

任务 7 — GitHub项目化用
  - 搜索相关项目 → 提取描述模板 → 标注来源 → 生成面试题

任务 8 — 低匹配度提示（<40%三选项）

【代码要求】
- Edge Function 需要限流和重试机制
- 匹配度雷达图使用 ECharts 或 Chart.js

【先列出文件清单】
```

---

### Phase 5：简历库与模块化

```
【接续 Phase 4，代码小浣熊 VSCode 插件模式】

请先读取现有代码和 docs/DESIGN.md 模块 F 的完整设计。

【Phase 5 任务：简历模块化】

任务 1 — 简历自动拆解引擎
  - 完整简历 Markdown 按标题关键词拆分为 8 类
  - LLM 辅助分类（无法匹配时）

任务 2 — 模块库管理 UI
  - 左侧：8类分类树 / 右侧：模块卡片列表
  - 全局搜索 + 每种模块计数

任务 3 — 模块质检（5维度）
  - 陈旧/简略/缺量化/无标签/时间冲突
  - 卡片角标(🔴🟡🟢)

任务 4 — 模块拼接生成简历
  - 拖拽式：左侧模块库/右侧实时预览
  - 排序/删除/替换

任务 5 — 5 套简历模板
  - 每套：布局CSS + 默认模块顺序
  - 模板缩略图预览 + 实时切换

任务 6 — PDF 导出
  - Edge Function: POST /functions/v1/export-pdf
  - Puppeteer 渲染 HTML → PDF

任务 7 — 投递快照
  - 投递时冻结当前简历版本

【先列出文件清单】
```

---

### Phase 6：投递与追踪看板

```
【接续 Phase 5，代码小浣熊 VSCode 插件模式】

请先读取现有代码和 docs/DESIGN.md 模块 E 的完整设计。

【Phase 6 任务：投递执行与追踪看板】

任务 1 — 投递跳转 + 日期记录
  - "去投递"按钮→新标签页打开源URL
  - 自动记录投递日期 + 写入 duplicate_keys

任务 2 — 表格视图（默认）
  - 列：标题/公司/平台/日期/状态(彩色标签)/提醒/操作
  - 排序/筛选/多选/6类批量操作
  - 状态颜色：灰蓝浅蓝黄绿红紫

任务 3 — 看板视图
  - 7状态分列 + 职位卡片 + 拖拽换列

任务 4 — 时间线视图
  - 投递日期垂直时间线 + 状态变化

任务 5 — 8 类智能提醒引擎
  - 7天/14天/面试24h/面试1h(强)/面试后3天/JD过期/批量投递后3天/自定义
  - 桌面通知 + 看板高亮

任务 6 — 看板可视化
  - 统计卡(总投递/面试率/转化率/平均响应)
  - 转化漏斗 / 平台对比柱状图 / 时间趋势折线图

【先列出文件清单】
```

---

### Phase 7：面试准备

```
【接续 Phase 6，代码小浣熊 VSCode 插件模式】

【Phase 7 任务：面试准备清单】

任务 1 — interview_prep_items CRUD
  - 表已在 Phase 0 创建，实现前后端 CRUD

任务 2 — AI 面试题生成 Edge Function
  - Qwen-Max：技术题/行为题/项目深挖题
  - 每题：类别/优先级(1-5)/预计时间

任务 3 — 精神风貌准备建议
  - JD模糊品质要求→准备策略+项目案例建议

任务 4 — UI：分类折叠 + 进度条 + 勾选
  - 来源标签(ai/manual/linked)

任务 5 — 联动
  - 简历新增→自动生成准备项
  - 面试邀请→自动加入清单

【先列出文件清单】
```

---

### Phase 8：多端完善 + 测试

```
【接续 Phase 7，代码小浣熊 VSCode 插件模式】

【Phase 8 任务：多端完善】

任务 1 — 多端同步（Supabase Realtime）
  - 订阅 resumes/applications/job_positions 变更
  - 编辑锁：5分钟TTL + 自动续锁

任务 2 — 桌面端独享编辑权
  - 编辑时设置锁 / 其他端只读提示

任务 3 — 浏览器插件完善
  - 表单半自动填充(BOSS/猎聘)
  - 状态同步检测

任务 4 — E2E 测试
  - 核心流程测试：注册→简历→JD→匹配→优化→投递→看板
  - 边界测试：黑名单/重复拦截/低匹配度

任务 5 — 使用代码小浣熊生成单元测试
  - 对关键模块批量生成测试用例

【先列出文件清单】
```

---

## 3. 办公小浣熊提示词库（PPT、数据分析、竞品调研）

### PPT 制作（3 轮对话）

```
=== 第 1 轮：用办公小浣熊做 PPT 大纲 ===

【上传文件】
请分析以下文件：
- d:/ALLPrograming/OPC/docs/DESIGN.md
- d:/ALLPrograming/OPC/docs/ARCHITECTURE.md
- d:/ALLPrograming/OPC/ppt/everydeliver-slides.md（已有的 Marp 草稿）

【任务】
为 EveryDeliver 项目设计一份 OPC 答辩 PPT 大纲。

要求：
- 总 15-20 页
- 10 分钟答辩时长
- 结构：痛点→方案→架构→核心亮点→技术栈→竞品→进度→总结

请给我每页的标题 + 3-5 个要点 + 是否需要配图。
```

```
=== 第 2 轮：用办公小浣熊生成架构图 ===

请基于 d:/ALLPrograming/OPC/docs/ARCHITECTURE.md 中的架构描述，
为 PPT 生成以下流程图和架构图（用 Mermaid 语法）：

1. 系统整体架构图（三端 → Supabase → AI 模型层）
2. 核心用户使用流程图
3. JD Parser 三层解析架构图
4. 投递状态机（7状态转换）
5. 匹配度评估公式图

每个图标注适用的 PPT 页码。
```

```
=== 第 3 轮：用办公小浣熊生成最终 PPT ===

请基于前两轮的大纲和架构图，生成一份完整的答辩 PPT。

【格式要求】
- 如果支持直接导出 .pptx，请导出为 d:/ALLPrograming/OPC/ppt/EveryDeliver答辩.pptx
- 如果不支持直接导出，请生成完整的 Markdown 文件，我使用 Marp CLI 转换

【设计要求】
- 配色：深蓝 #1a365d / 白 #ffffff / 橙色强调 #ed8936
- 每页标题醒目
- 关键数字用大号字体
- 架构图用 Mermaid 渲染
- 代码/技术栈用等宽字体
```

---

### 项目简介生成

```
=== 项目简介三版本 ===

请基于 d:/ALLPrograming/OPC/docs/ 下的所有设计文档，
为 EveryDeliver 生成三个版本的项目简介：

1. 一句话版（<50字）
   - 用于：海报/展板/GitHub description
   - 要求：抓眼球，突出差异化

2. 1分钟版（150-200字）
   - 用于：路演开场/电梯演讲
   - 要求：痛点→方案→亮点，逻辑清晰

3. 完整版（800-1000字）
   - 用于：提交材料/评委阅读
   - 要求：技术深度 + 设计细节 + 数据支撑

输出到 d:/ALLPrograming/OPC/docs/PROJECT_BRIEF.md
```

---

### 一图读懂（项目海报/信息图）

```
=== 用办公小浣熊生成"一图读懂 EveryDeliver" ===

请基于设计文档，生成一张小红书风格的"一图读懂 EveryDeliver"信息图。

包含：
- 一句话定位（大字标题）
- 解决的 2 个痛点（左右对比）
- 核心流程（上传简历 → 导入JD → AI优化 → 投递 → 追踪）
- 3 个核心亮点（小图标 + 一句话）
- 技术栈标签云
- GitHub 链接 + QR 码占位

输出：PNG 图片到 d:/ALLPrograming/OPC/ppt/infographic.png
```

---

### 竞品调研（浏览器自动化）

```
=== 用办公小浣熊浏览器自动化做竞品调研 ===

请用浏览器自动化功能，帮我调研以下竞品的功能：

竞品列表：
1. BOSS直聘 — 投递追踪功能
2. 猎聘 — 简历优化功能
3. 超级简历 — AI简历功能

对每个竞品，请分析：
- 核心功能点
- 定价模式
- 优点/缺点
- 与 EveryDeliver 的差异

输出分析报告到 d:/ALLPrograming/OPC/docs/COMPETITOR_ANALYSIS.md
```

---

### 投递数据分析（如有测试数据）

```
=== 用办公小浣熊分析投递数据 ===

【上传文件】
d:/ALLPrograming/OPC/test-data/applications.csv（模拟投递数据）

【任务】
1. 数据清洗与预处理
2. 分析指标：
   - 投递→面试转化率
   - 各平台效果对比（BOSS vs 猎聘 vs 官网）
   - 各时间段投递回复率
   - 各薪资段面试率
3. 生成分析报告 + 可视化图表
4. 将图表导出为 PNG，用于 PPT
```

---

## 4. 小浣熊能力 × 项目需求 精准匹配矩阵

### 代码小浣熊负责

| 项目需求 | 代码小浣熊能力 | 使用模式 |
|---------|--------------|---------|
| 初始化项目结构 | 代码生成（90+语言） | VSCode 侧边栏 |
| Tauri + React 开发 | 代码补全 + 跨文件修改 | 编辑器内 GUI |
| Supabase Edge Functions | TypeScript 代码生成 | VSCode |
| 数据库迁移 SQL | SQL 生成 + 注释 | VSCode |
| 浏览器插件 MV3 | JavaScript/TS 生成 | VSCode |
| 单元测试 | 批量测试生成 | VSCode |
| 代码重构 | 冗余检测 + 优化建议 | VSCode |
| Bug 修复 | 代码修正 | VSCode |
| 代码注释 | 一键文档注释 | VSCode |

### 办公小浣熊桌面版负责

| 项目需求 | 办公小浣熊能力 | 使用模式 |
|---------|--------------|---------|
| PPT 答辩制作 | PPT生成 + PPTX导出 | 桌面端对话 |
| 项目信息图 | 一图读懂（小红书风格） | 桌面端对话 |
| 项目简介三版本 | 内容生成 + 信息源校对 | 桌面端对话 |
| 竞品调研 | 一句话操控浏览器 | Quick Bar |
| 市场数据收集 | 浏览器自动化抓取 | 桌面端对话 |
| 测试数据分析 | 数据分析 + 可视化 | 上传 CSV |
| 投递报表 | 定时任务（如周报） | 桌面端 |
| 设计文档管理 | 本地文件直接处理 | 授权目录 |
| 团队协作 | 打通飞书 | 一键导出 |
| 快速查技术问题 | Quick Bar Ctrl+K | 全局唤起 |

---

## 5. 办公小浣熊桌面智能体 6 大能力在本项目中的应用

| # | 能力 | 本项目中如何使用 |
|---|------|----------------|
| ① | 直接处理本地文件 | 授权 `d:/ALLPrograming/OPC/` 目录，让办公小浣熊直接读取设计文档和代码 |
| ② | 一句话操控浏览器 | 竞品调研、JD 格式分析（看 BOSS/猎聘页面结构）、技术文档搜索 |
| ③ | Quick Bar 全局唤起 | 写代码时选中一段文字 → Ctrl+K → "翻译成英文简历"/"优化这段项目描述" |
| ④ | 无缝打通飞书 | 设计文档 → 一键导出为飞书文档，与队友/导师协作 |
| ⑤ | 本地记忆 | 记住你的技术栈偏好、输出习惯、常用分析结构，越用越精准 |
| ⑥ | 定时任务 | 每周五自动分析 GitHub commit 活跃度，生成进度报告 |

---

## 6. 代码小浣熊的关键使用技巧

### 技巧 1：跨文件多任务修改（2.0 核心能力）

```
请帮我完成以下跨文件修改：
- 将 resumes 表的字段名 full_text 改为 content
- 同步修改所有引用该字段的：
  - Edge Functions
  - 前端组件
  - TypeScript 类型定义
  - SQL 迁移文件

要求：列出所有受影响的文件和行号，确认后再执行。
```

### 技巧 2：批量生成测试用例

```
请为以下模块批量生成单元测试：
- JD Parser 规则解析器（BOSS直聘/猎聘）
- 三层过滤引擎
- 匹配度计算引擎
- 黑名单自动检测正则

每个函数至少覆盖正常/边界/异常三种情况。
使用 Jest 框架。
```

### 技巧 3：需求文档驱动开发

```
请基于 d:/ALLPrograming/OPC/docs/DESIGN.md 中模块 G 的设计，
帮我编写完整的需求文档（PRD），然后逐步转化为代码。

需求文档包含：
- 功能概述
- 输入/输出定义
- 边界条件
- 错误处理策略
- 接口定义
```

---

## 7. 提交节奏与 OPC 作品展示建议

```
GitHub 提交时间线：

Week 1-2  ─→ Phase 0 提交  "feat: 基础设施搭建"
Week 3-4  ─→ Phase 1+2 提交 "feat: 用户画像 + JD Parser"
Week 5-7  ─→ Phase 3+4 提交 "feat: 职位过滤 + AI匹配优化（核心亮点）"
Week 8-10 ─→ Phase 5+6 提交 "feat: 简历模块化 + 投递看板"
Week 11-12 ─→ Phase 7+8 提交 "feat: 面试准备 + 多端完善"
比赛前3天 ─→ PPT + 项目简介 + 一图读懂 提交
比赛前1天 ─→ 用办公小浣熊做最终检查和分析

每个 commit message 用英文 feat:/fix:/docs: 规范
```

---

## 8. 快速启动：打开两个窗口

```
窗口 1：VSCode + 代码小浣熊插件
  → 打开 d:/ALLPrograming/OPC/
  → 输入 Phase 0 提示词
  → 生成代码 → 调试 → 提交

窗口 2：办公小浣熊桌面版
  → 授权 d:/ALLPrograming/OPC/ 目录
  → 同时进行 PPT 制作、竞品调研、数据分析
  → 两个窗口互不阻塞
```
